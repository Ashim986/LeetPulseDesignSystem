---
phase: 01-readme-naming-foundation-and-component-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - NAME-01
  - NAME-02
  - NAME-03
  - NAME-04
  - USAGE-06

must_haves:
  truths:
    - "Zero occurrences of 'FocusDesignSystem' anywhere in README.md — prose, headings, and code blocks"
    - "All import statements use correct module names: LeetPulseDesignSystem, LeetPulseDesignSystemCore, LeetPulseDesignSystemState, LeetPulseDesignSystemComponents"
    - "All file path references match actual filesystem (Sources/LeetPulseDesignSystem*, not Sources/FocusDesignSystem*)"
    - "SPM installation block includes the correct repository URL (https://github.com/Ashim986/LeetPulseDesignSystem), iOS 26 and macOS 14 deployment targets"
    - "Module dependency decision table explains Core vs State vs Components vs umbrella import"
  artifacts:
    - path: "README.md"
      provides: "Renamed README with correct module names, paths, SPM block, and module decision table"
      contains: "LeetPulseDesignSystem"
  key_links:
    - from: "README.md import statements"
      to: "Package.swift module names"
      via: "import LeetPulseDesignSystem* matches Package.swift target names"
      pattern: "import LeetPulseDesignSystem"
    - from: "README.md file path references"
      to: "Sources/ directory structure"
      via: "Sources/LeetPulseDesignSystem* paths match filesystem"
      pattern: "Sources/LeetPulseDesignSystem"
---

<objective>
Replace all FocusDesignSystem references in README.md with LeetPulseDesignSystem, fix all import paths and file path references, add SPM installation block with deployment targets, and add module dependency decision table.

Purpose: Establish the naming foundation that all downstream documentation updates depend on. Every code block, heading, and path in README must use the canonical LeetPulseDesignSystem name before catalog and theme sections are added.

Output: README.md with correct naming throughout, proper SPM installation section, and module import decision table.
</objective>

<execution_context>
@/Users/ashimdahal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ashimdahal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-RESEARCH.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-CONTEXT.md
@README.md
@Package.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all FocusDesignSystem references and fix file paths</name>
  <files>README.md</files>
  <action>
Read the current README.md. Replace every occurrence of "FocusDesignSystem" with "LeetPulseDesignSystem" in ALL contexts:

1. **Title heading** (line 1): `# FocusDesignSystem` -> `# LeetPulseDesignSystem`
2. **Opening paragraph** (line 3): `FocusDesignSystem is a...` -> `LeetPulseDesignSystem is a...`
3. **Package Structure section**: Replace all 4 module name references:
   - `FocusDesignSystemCore` -> `LeetPulseDesignSystemCore`
   - `FocusDesignSystemState` -> `LeetPulseDesignSystemState`
   - `FocusDesignSystemComponents` -> `LeetPulseDesignSystemComponents`
   - `FocusDesignSystem` (umbrella) -> `LeetPulseDesignSystem`
4. **Themes code block** (line 31): `import FocusDesignSystem` -> `import LeetPulseDesignSystem`
5. **Components code block** (line 41): `import FocusDesignSystem` -> `import LeetPulseDesignSystem`
6. **Documentation section** (line 185): `Sources/FocusDesignSystemComponents/Examples/DSSampleScreens.swift` -> `Sources/LeetPulseDesignSystemComponents/Examples/DSSampleScreens.swift`

After replacement, run `grep -n "FocusDesignSystem" README.md` to verify zero occurrences remain.

IMPORTANT: Do NOT use blind find-and-replace. Read the README, identify each occurrence, and replace with context awareness. The file path on line 185 requires the full path correction, not just a name swap.
  </action>
  <verify>Run `grep -c "FocusDesignSystem" README.md` — must return 0. Run `grep -n "LeetPulseDesignSystem" README.md` to confirm replacements are present in headings, prose, code blocks, and file paths.</verify>
  <done>Zero occurrences of "FocusDesignSystem" in README.md. All headings, prose, import statements, module names, and file paths use "LeetPulseDesignSystem" variants.</done>
</task>

<task type="auto">
  <name>Task 2: Add SPM installation block and module dependency decision table</name>
  <files>README.md</files>
  <action>
After the rename pass is complete, add two new sections to README.md:

**1. Installation section** — Add immediately after the opening paragraph and repository URL line (before "## Goals"):

```markdown
## Installation

Add LeetPulseDesignSystem via Swift Package Manager:

```swift
// In your Package.swift dependencies:
.package(url: "https://github.com/Ashim986/LeetPulseDesignSystem", from: "1.0.0")
```

**Minimum deployment targets:**
- iOS 26+
- macOS 14+
```

The repository URL `https://github.com/Ashim986/LeetPulseDesignSystem` is the confirmed correct URL.

**2. Module dependency decision table** — Replace the existing "## Package Structure" section content. Keep the heading but rewrite the body:

Start with a brief paragraph: "LeetPulseDesignSystem is organized into four Swift modules. The umbrella module re-exports all three sub-modules via `@_exported import`, so most apps only need a single import."

Then add the module dependency graph (text, not code):
- `LeetPulseDesignSystemCore` — tokens, themes, DSThemeProvider
- `LeetPulseDesignSystemState` (depends on Core) — state machines, validation rules
- `LeetPulseDesignSystemComponents` (depends on Core + State) — all DS* UI components
- `LeetPulseDesignSystem` (umbrella) — re-exports all three modules

Then add the decision table:

| If you need | Import |
|-------------|--------|
| Just tokens, themes, DSThemeProvider | `import LeetPulseDesignSystemCore` |
| State machines, validation rules | `import LeetPulseDesignSystemState` |
| UI components (DS* views) | `import LeetPulseDesignSystemComponents` |
| Everything (recommended for apps) | `import LeetPulseDesignSystem` |

This satisfies USAGE-06 (module dependency explanation) and NAME-04 (SPM installation with correct package name and URL).
  </action>
  <verify>Verify README.md contains: (1) `## Installation` section with SPM `.package(url:` block and deployment targets, (2) module dependency paragraph with all 4 module names, (3) decision table with 4 rows mapping use cases to imports. Run `grep -n "import LeetPulseDesignSystem" README.md` to confirm import examples are present.</verify>
  <done>README.md has SPM installation block with URL and platform targets (iOS 26+, macOS 14+), module dependency explanation, and a 4-row decision table mapping use cases to correct module imports.</done>
</task>

</tasks>

<verification>
1. `grep -c "FocusDesignSystem" README.md` returns 0
2. `grep -c "LeetPulseDesignSystem" README.md` returns 10+ (multiple sections reference the name)
3. README.md contains `## Installation` section with `.package(url:` syntax
4. README.md contains module decision table with all 4 module names
5. File path reference to DSSampleScreens.swift uses `Sources/LeetPulseDesignSystemComponents/`
</verification>

<success_criteria>
- Zero FocusDesignSystem references in README.md
- All import statements reference correct LeetPulseDesignSystem* module names
- SPM installation block present with repository URL and deployment targets
- Module dependency decision table present with 4 import options
- All file paths match actual filesystem structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-readme-naming-foundation-and-component-catalog/01-01-SUMMARY.md`
</output>
