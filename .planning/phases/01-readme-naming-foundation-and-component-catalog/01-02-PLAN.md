---
phase: 01-readme-naming-foundation-and-component-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: true
requirements:
  - CATL-01
  - CATL-02
  - CATL-03
  - CATL-04

must_haves:
  truths:
    - "Every public DS* component from the filesystem appears in the README catalog — count matches filesystem audit (73 files)"
    - "Components organized into exactly 5 groups: Primitives, Form Controls, Navigation, Feedback, Visualization"
    - "Each group has a one-line description explaining when to reach for it"
    - "Mobile-specific components (from Sources/LeetPulseDesignSystemComponents/Mobile/) have an iOS/iPadOS badge/label in the Platform column"
    - "Components listed alphabetically within each group"
  artifacts:
    - path: "README.md"
      provides: "Complete component catalog with 5 grouped mini tables"
      contains: "## Component Catalog"
  key_links:
    - from: "README.md component catalog"
      to: "Sources/LeetPulseDesignSystemComponents/"
      via: "Every DS*.swift file (excl. Examples/) appears in a catalog table"
      pattern: "DS[A-Z]"
    - from: "README.md Platform column"
      to: "Sources/LeetPulseDesignSystemComponents/Mobile/"
      via: "Mobile/ files get iOS/iPadOS label, others get All"
      pattern: "iOS/iPadOS"
---

<objective>
Replace the existing flat component catalog in README.md with a comprehensive, grouped catalog covering all 73 DS*.swift files from the filesystem, organized into 5 functional groups with mini tables.

Purpose: A developer browsing the README can find every public component, understand what it does, and know which platform it targets. The catalog is the central reference for what the design system offers.

Output: README.md with complete component catalog — 5 groups, mini tables (Name | Description | Platform), alphabetical within groups, mobile components badged iOS/iPadOS.
</objective>

<execution_context>
@/Users/ashimdahal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ashimdahal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-RESEARCH.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-CONTEXT.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-01-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify component inventory from filesystem</name>
  <files>README.md</files>
  <action>
Before writing the catalog, verify the component inventory against the actual filesystem. Run:

```bash
find Sources/LeetPulseDesignSystemComponents -name "DS*.swift" ! -path "*/Examples/*" | sort
```

Cross-reference the output against the research inventory (01-RESEARCH.md Component Inventory section). For each file, confirm it contains a `public struct` or `public enum` by running:

```bash
grep -l "^public struct\|^public class\|^public enum" Sources/LeetPulseDesignSystemComponents/*.swift Sources/LeetPulseDesignSystemComponents/Mobile/*.swift
```

Also verify the Core and State DS* types that should NOT appear in the component catalog but may need mention:
- `Sources/LeetPulseDesignSystemCore/DS*.swift` — tokens, not components (DSTheme, DSVizColors, DSGradients, DSLayout, DSMobileTokens)
- `Sources/LeetPulseDesignSystemState/DS*.swift` — validation types, not UI components

Record the final count and any discrepancies from the research document. The research says 73 files (52 cross-platform + 21 Mobile). Confirm this.

Do NOT write to README yet in this task — only verify the inventory.
  </action>
  <verify>The filesystem scan produces a definitive component list. Count matches or updates the research figure. Any new or missing files are noted.</verify>
  <done>Complete, verified component inventory list ready for catalog writing. Any discrepancies between research and filesystem resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Write grouped component catalog in README.md</name>
  <files>README.md</files>
  <action>
Replace the existing "## Component Catalog" section in README.md (currently a flat bullet list of ~25 items) with a new structured catalog. Also remove the "## Primitives (Intended Scope)" section (lines 188-196) since its content is now integrated into the catalog groups.

**Structure per user decision (from CONTEXT.md):**
- 5 groups: Primitives, Form Controls, Navigation, Feedback, Visualization
- No separate Mobile-specific group — mobile components go into their functional group with inline iOS/iPadOS badge
- Mini table format: Name | Description | Platform
- Alphabetical within each group
- One-liner intro per group

**Add a total count summary** at the top of the catalog section (Claude's discretion — include it for developer orientation).

**Group assignments** (from 01-RESEARCH.md, verified by Task 1):

**Primitives** — Low-level building blocks and shapes, composable into higher-level components:

| Component | Description | Platform |
|-----------|-------------|----------|
| DSArrow | Directional arrow shape | All |
| DSBubble | Circular label badge for compact values | All |
| DSCard | Surface container with elevation/outline styles | All |
| DSConsoleOutput | Monospaced text output panel | All |
| DSCurvedArrow | Curved directional arrow with configurable offset | All |
| DSExpandableText | Truncated text with expand/collapse toggle | All |
| DSImage | Themed image container with loading states | All |
| DSPointerBadge | Pointer marker badge for graph visualization | All |
| DSText | Themed text label with DSTheme typography and color | All |

**Form Controls** — Input, selection, and validation components:

| Component | Description | Platform |
|-----------|-------------|----------|
| DSActionButton | Compact action trigger with icon support | All |
| DSButton | Primary action button with loading, disabled, and icon states | All |
| DSFormField | Form field wrapper with label and validation display | All |
| DSIconButton | Icon-only button for toolbar actions | All |
| DSPicker | Value picker with configurable options | All |
| DSPomodoroSegmentedControl | Pomodoro-specific segmented timer control | iOS/iPadOS |
| DSSearchBar | Search input with clear button and keyboard handling | iOS/iPadOS |
| DSSegmentedControl | Horizontal segmented selection control | All |
| DSSelect | Dropdown-style selection with options list | All |
| DSTextArea | Multi-line text input field | All |
| DSTextField | Single-line text input with optional validation state | All |
| DSTextValidation | Text input validation policy definitions | All |
| DSToggle | On/off toggle switch | All |

**Navigation** — Structural navigation and layout scaffolding:

| Component | Description | Platform |
|-----------|-------------|----------|
| DSBottomTabBar | Bottom tab bar for iOS tab navigation | iOS/iPadOS |
| DSNavItem | Model type for navigation items (used by tab bar, sidebar, scaffolds) | All |
| DSScaffolds | Layout scaffolds: DSTabScaffold and DSSidebarScaffold | All |
| DSSidebar | Vertical sidebar navigation list | All |
| DSSidebarNav | Mobile-optimized sidebar navigation | iOS/iPadOS |
| DSTabBar | Horizontal tab bar navigation | All |

**Feedback** — Status indicators, alerts, progress, and informational displays:

| Component | Description | Platform |
|-----------|-------------|----------|
| DSAlert | Dismissible alert banner with severity styles | All |
| DSBadge | Status badge with text and color variants | All |
| DSCompactHeaderBar | Condensed header bar for secondary screens | All |
| DSDailyGoalCard | Daily goal progress card | iOS/iPadOS |
| DSEmptyState | Placeholder view for empty data states | All |
| DSFocusTimeCard | Focus session time display card | iOS/iPadOS |
| DSHeader | Section header with title and optional subtitle | All |
| DSHeaderBar | Mobile navigation header bar | iOS/iPadOS |
| DSInlineErrorBanner | Inline error message display | All |
| DSListRow | Configurable list row with leading/trailing content | All |
| DSMetricCard | Metric display card with value, trend, and detail | All |
| DSMetricCardView | Mobile-optimized metric card layout | iOS/iPadOS |
| DSProblemCard | Problem/challenge display card | iOS/iPadOS |
| DSProgressHeader | Header with integrated progress indicator | All |
| DSProgressRing | Circular progress indicator ring | All |
| DSScheduleRow | Schedule entry row display | iOS/iPadOS |
| DSSectionHeader | Section divider with title | All |
| DSSettingsRow | Settings menu row with label and accessory | iOS/iPadOS |
| DSSignOutButton | Sign out action button | iOS/iPadOS |
| DSStartFocusCTA | Call-to-action for starting focus sessions | iOS/iPadOS |
| DSStatusCard | Status information card with icon and message | All |
| DSStreakBadge | Streak counter badge display | iOS/iPadOS |
| DSSurfaceCard | Elevated surface card container | iOS/iPadOS |
| DSTaskRow | Task list row with completion state | iOS/iPadOS |
| DSTimerRing | Countdown timer ring display | iOS/iPadOS |
| DSToast | Temporary notification toast message | All |

**Visualization** — Data display, charts, and algorithm visualization:

| Component | Description | Platform |
|-----------|-------------|----------|
| DSBarChart | Vertical bar chart for data comparison | iOS/iPadOS |
| DSCalendarGrid | Calendar grid for date-based data display | iOS/iPadOS |
| DSCodeViewer | Syntax-highlighted code display panel | iOS/iPadOS |
| DSGraphView | Node-and-edge graph visualization (Canvas-rendered) | All |
| DSLineChart | Line chart for trend data display | iOS/iPadOS |
| DSTreeGraphView | Tree structure visualization (Canvas-rendered) | All |

**Mobile components note:** Add a brief note after the catalog: "Components marked iOS/iPadOS reside in `Sources/LeetPulseDesignSystemComponents/Mobile/` and use `DSMobileTokens` from `LeetPulseDesignSystemCore` instead of `DSTheme`. They compile on all platforms but are designed for iOS and iPadOS layouts."

**Also remove** the old "## Primitives (Intended Scope)" section entirely — its content about cross-platform reuse intent is subsumed by the catalog Platform column and the mobile note.

**Platform column labeling:** Use "All" for cross-platform and "iOS/iPadOS" for Mobile/ components (Claude's discretion per CONTEXT.md).

**Description source:** Write fresh one-liner descriptions for README audience clarity rather than pulling from sparse source doc comments (Claude's discretion per CONTEXT.md).

Note: DSBadge+Difficulty.swift is an extension file, not a standalone component — do not list it as a separate catalog entry. Its functionality extends DSBadge.
  </action>
  <verify>
1. Count components in the new catalog: should total 73 entries across all 5 groups (verify against filesystem scan from Task 1; adjust if Task 1 found discrepancies).
2. Run `grep -c "iOS/iPadOS" README.md` — should match the count of Mobile/ components (21).
3. Verify each group has a one-liner intro sentence before its table.
4. Verify alphabetical ordering within each group by visual inspection.
5. Verify "## Primitives (Intended Scope)" section is removed.
  </verify>
  <done>README.md has a complete component catalog with 5 groups, mini tables (Name | Description | Platform), all 73 DS* components listed, mobile components labeled iOS/iPadOS, alphabetical within groups, and one-liner group descriptions.</done>
</task>

</tasks>

<verification>
1. `find Sources/LeetPulseDesignSystemComponents -name "DS*.swift" ! -path "*/Examples/*" | wc -l` matches the number of entries in the catalog
2. Every file from the filesystem scan appears in exactly one catalog group
3. `grep -c "iOS/iPadOS" README.md` matches the number of Mobile/ files that are view components
4. Each of the 5 group headings has a one-liner description
5. The old flat bullet list catalog and "Primitives (Intended Scope)" section are gone
</verification>

<success_criteria>
- All public DS* components from filesystem listed in catalog
- 5 groups: Primitives, Form Controls, Navigation, Feedback, Visualization
- Each group has one-line intro description
- Mini table format with Name | Description | Platform columns
- Mobile components labeled iOS/iPadOS in Platform column
- Alphabetical ordering within each group
- Mobile components note explaining DSMobileTokens usage
</success_criteria>

<output>
After completion, create `.planning/phases/01-readme-naming-foundation-and-component-catalog/01-02-SUMMARY.md`
</output>
