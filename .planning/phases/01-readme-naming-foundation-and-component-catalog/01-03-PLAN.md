---
phase: 01-readme-naming-foundation-and-component-catalog
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: true
requirements:
  - USAGE-03
  - USAGE-04

must_haves:
  truths:
    - "A developer can follow the theme setup section and wire up DSThemeProvider, light/dark selection, and @Environment(\\.dsTheme) access from README alone"
    - "A developer can look up any token name (colors, spacing, radii, typography, viz colors) and know its value or purpose from the token reference"
    - "Theme setup section uses concept-first approach: explains what the theme system does before showing code"
  artifacts:
    - path: "README.md"
      provides: "Theme setup guide and token reference tables"
      contains: "DSThemeProvider"
  key_links:
    - from: "README.md theme setup code"
      to: "Sources/LeetPulseDesignSystemCore/DSTheme.swift"
      via: "DSThemeProvider, DSTheme.light, DSTheme.dark, @Environment(\\.dsTheme) all verified from source"
      pattern: "DSThemeProvider"
    - from: "README.md token reference tables"
      to: "Sources/LeetPulseDesignSystemCore/DSTheme.swift"
      via: "Token names and values match DSColors, DSTypography, DSSpacing, DSRadii, DSShadow properties"
      pattern: "theme\\.(colors|typography|spacing|radii|shadow)"
    - from: "README.md viz colors reference"
      to: "Sources/LeetPulseDesignSystemCore/DSVizColors.swift"
      via: "8 Okabe-Ito color names and 3 semantic aliases from source"
      pattern: "vizColors"
---

<objective>
Add comprehensive theme setup documentation and token reference tables to README.md. The theme section explains DSThemeProvider, light/dark selection, and environment access with a concept-first approach. The token reference lists all token names developers can use in custom components.

Purpose: A developer reading the README can set up theming and use design tokens in custom views without opening source files. This is the "how to use the design system" section.

Output: README.md with theme setup section and complete token reference tables (colors, typography, spacing, radii, shadow, viz colors).
</objective>

<execution_context>
@/Users/ashimdahal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ashimdahal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-RESEARCH.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-CONTEXT.md
@.planning/phases/01-readme-naming-foundation-and-component-catalog/01-01-SUMMARY.md
@README.md
@Sources/LeetPulseDesignSystemCore/DSTheme.swift
@Sources/LeetPulseDesignSystemCore/DSVizColors.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify theme API and token values from source</name>
  <files>README.md</files>
  <action>
Before writing documentation, read and verify the actual API from source files:

1. Read `Sources/LeetPulseDesignSystemCore/DSTheme.swift` to confirm:
   - `DSThemeProvider<Content: View>` init signature (takes `theme: DSTheme` and `@ViewBuilder content: () -> Content`)
   - `DSTheme.light` and `DSTheme.dark` static properties exist
   - `EnvironmentValues.dsTheme` environment key exists
   - `DSColors` property names: enumerate all 12+ color properties
   - `DSTypography` property names: confirm 5 roles (title, subtitle, body, caption, mono) and their Font types
   - `DSSpacing` property names and values: xs=4, sm=8, md=12, lg=16, xl=24
   - `DSRadii` property names and values: sm=6, md=10, lg=16, pill=999
   - `DSShadow` fields: color, radius, x, y
   - `DSThemeKind` enum cases

2. Read `Sources/LeetPulseDesignSystemCore/DSVizColors.swift` to confirm:
   - 8 named Okabe-Ito colors and their property names
   - 3 semantic aliases (highlight, selected, error) and what they map to
   - Light vs dark variant support

3. Read `Sources/LeetPulseDesignSystemCore/DSColors.swift` if it exists separately, to capture any utility extension colors (surfaceClear, foregroundOnViz, textDisabled).

Record all verified token names and values. Do NOT write to README yet.
  </action>
  <verify>All token property names and values confirmed from source. Any discrepancies from the 01-RESEARCH.md document noted.</verify>
  <done>Complete verified token inventory: all color names, typography roles, spacing values, radii values, shadow fields, viz color names, and semantic aliases confirmed from Swift source files.</done>
</task>

<task type="auto">
  <name>Task 2: Write theme setup section and token reference tables in README.md</name>
  <files>README.md</files>
  <action>
Rewrite the existing "## Themes" section in README.md and add a new "## Token Reference" section. Per user decision (CONTEXT.md): concept-first approach — explain what the theme system does, then show code.

**1. Rewrite "## Themes" section:**

Start with a concept paragraph explaining:
- LeetPulseDesignSystem uses a centralized theme system
- All components read design tokens (colors, typography, spacing) from the theme
- The theme is injected via SwiftUI environment and accessed via `@Environment(\.dsTheme)`
- Two built-in themes: `.light` and `.dark`

Then show the setup code (verified from Task 1):

```swift
import LeetPulseDesignSystem

// Wrap your root view with DSThemeProvider
DSThemeProvider(theme: .light) {
    ContentView()
}
```

Then show how to access tokens in a custom view:

```swift
struct MyView: View {
    @Environment(\.dsTheme) var theme

    var body: some View {
        Text("Hello")
            .foregroundStyle(theme.colors.textPrimary)
            .font(theme.typography.body)
            .padding(theme.spacing.md)
    }
}
```

Then show dark mode switching:

```swift
// Switch to dark theme
DSThemeProvider(theme: .dark) {
    ContentView()
}
```

**2. Add "## Token Reference" section** (after Themes, before Component Catalog):

Per user decision, full tables since token counts are small. Per CONTEXT.md: "Claude decides full table vs summary based on actual token count" — counts are under 15 per group, so full tables are appropriate.

Access pattern: `@Environment(\.dsTheme) var theme`, then `theme.colors.X`, `theme.typography.X`, `theme.spacing.X`, `theme.radii.X`, `theme.shadow.X`.

**Colors** — List all DSColors properties with their semantic purpose. Use a table:

| Token | Purpose |
|-------|---------|
| `theme.colors.primary` | Primary brand color |
| `theme.colors.secondary` | Secondary accent |
| ... (all 12+ color properties from Task 1 verification) |

Include the 3 utility extensions (surfaceClear, foregroundOnViz, textDisabled) if confirmed.

**Typography** — 5 roles:

| Token | Usage |
|-------|-------|
| `theme.typography.title` | Page and section titles |
| `theme.typography.subtitle` | Secondary headings |
| `theme.typography.body` | Body text |
| `theme.typography.caption` | Small labels and metadata |
| `theme.typography.mono` | Code and monospaced text |

**Spacing** — 5 tokens with values:

| Token | Value | Usage |
|-------|-------|-------|
| `theme.spacing.xs` | 4 pt | Tight spacing between related elements |
| `theme.spacing.sm` | 8 pt | Default inner padding |
| `theme.spacing.md` | 12 pt | Standard component spacing |
| `theme.spacing.lg` | 16 pt | Section separation |
| `theme.spacing.xl` | 24 pt | Large gaps between sections |

**Corner Radii** — 4 tokens:

| Token | Value | Usage |
|-------|-------|-------|
| `theme.radii.sm` | 6 pt | Subtle rounding (badges, tags) |
| `theme.radii.md` | 10 pt | Standard component rounding (cards, buttons) |
| `theme.radii.lg` | 16 pt | Large rounding (modals, panels) |
| `theme.radii.pill` | 999 pt | Fully rounded (pills, circular buttons) |

**Shadow** — 4 fields:

| Field | Purpose |
|-------|---------|
| `theme.shadow.color` | Shadow color (opacity varies by theme) |
| `theme.shadow.radius` | Blur radius |
| `theme.shadow.x` | Horizontal offset |
| `theme.shadow.y` | Vertical offset |

**Visualization Colors (Okabe-Ito)** — List the 8 named colors and 3 semantic aliases from DSVizColors. Access via `theme.vizColors.X`:

| Token | Description |
|-------|-------------|
| (8 color names from Task 1 verification) | Colorblind-safe palette colors |
| `theme.vizColors.highlight` | Semantic alias for highlighted data |
| `theme.vizColors.selected` | Semantic alias for selected data |
| `theme.vizColors.error` | Semantic alias for error data |

Add a brief note: "Visualization colors follow the Okabe-Ito palette, designed for colorblind accessibility. Both light and dark theme variants are provided."

IMPORTANT: All token names MUST be verified from the actual Swift source in Task 1. Do not guess property names. If a property name differs from what the research says, use the source-verified name.
  </action>
  <verify>
1. README.md "## Themes" section contains concept paragraph, DSThemeProvider code, @Environment access code, and dark mode example.
2. README.md "## Token Reference" section contains tables for: Colors, Typography, Spacing, Corner Radii, Shadow, Visualization Colors.
3. All token names in tables match actual DSTheme.swift property names — spot-check 3-4 tokens by running `grep "propertyName" Sources/LeetPulseDesignSystemCore/DSTheme.swift`.
4. Viz color names match DSVizColors.swift — run `grep "public" Sources/LeetPulseDesignSystemCore/DSVizColors.swift` to verify.
  </verify>
  <done>README.md has concept-first theme setup section with DSThemeProvider, light/dark selection, and @Environment access. Token reference tables list all colors, typography roles, spacing values, radii values, shadow fields, and visualization colors with verified property names.</done>
</task>

</tasks>

<verification>
1. Theme setup section explains the concept before showing code
2. DSThemeProvider, DSTheme.light, DSTheme.dark, @Environment(\.dsTheme) all appear in the Themes section
3. Token reference has tables for all 6 token groups (colors, typography, spacing, radii, shadow, viz colors)
4. Every token name in the tables can be found in the corresponding Swift source file
5. Viz colors section mentions Okabe-Ito and lists all 8 colors + 3 aliases
</verification>

<success_criteria>
- Theme setup instructions cover DSThemeProvider, light/dark selection, and @Environment(\.dsTheme) access
- Concept-first approach: prose explanation before code examples
- Token reference lists all token names across colors, typography, spacing, radii, shadow
- Visualization colors documented with Okabe-Ito palette reference
- All token names verified against actual Swift source — not from memory
</success_criteria>

<output>
After completion, create `.planning/phases/01-readme-naming-foundation-and-component-catalog/01-03-SUMMARY.md`
</output>
